# angular-chrts (Angular Charts) still declares Angular ^20.3.0 as a peer while this
# workspace runs Angular 21. The library itself is Angular-21 compatible; only the peer
# range is stale. Revisit when angular-chrts publishes a v21 peer range.
legacy-peer-deps=true
